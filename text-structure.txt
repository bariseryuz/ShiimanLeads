//Purpose of this structure is to detect lead ingredients right after the anouncement of the targeted sources that are provided below.


/*
//Frontend - Express
Storage - SQ Database 
Backend - Node.js */




//Website (Main UI) => Admin Panel => 


/*mkdir lead-generation / cd lead-generation 
npm init -y (Project initialization)
npm install axios cheerio sqlite3 node-cron dotenv winston axios-retry(Modules needs to be installed.)
package.json (Dependencies will be added here)
.env (Environment variables will be stored here)
sources.json (Targeted sources will be stored here)
index.js (Main application logic will be written here)
utils/
   ├── fetcher.js (polite way to download pages)
   ├── parser.js (Turns Raw Data into structured data)
   ├── db.js (Saves the leads and makes sure it is not duplicated)

   

/*axios - Fetched the web browsers.
cheerio - Reads HTML from pages and extracts info.
crypto-js - Creates unique "fingerprints"
node-cron - Schedules checks every X minutes/hours
dotenv - Handles secrets.
winston - Logs events for monitoring.
axios-retry - Auto-Retries failed requests.
sqlite3 - Stores leads in a lightweight database.
pm2- Keeps the script running automatically even if it crashes .