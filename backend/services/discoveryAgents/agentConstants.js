/**
 * Logical multi-agent pipeline (same process, same API keys — different prompts/steps).
 * Agent A = Find | Agent B = Read | Agent C = Verify / Shape
 */
module.exports = {
  AGENT_FIND: 'find',
  AGENT_READ: 'read',
  AGENT_VERIFY: 'verify'
};
